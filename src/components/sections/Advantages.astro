---
import Container from '@/components/ui/Container.astro';
import SectionTitle from '@/components/ui/SectionTitle.astro';
import FeatureCard from '@/components/ui/FeatureCard.astro';
import { advantages } from '@/data/site';
---

<section id="advantages" class="py-12 sm:py-16">
  <Container class="space-y-8">
    <SectionTitle
      eyebrow={advantages.section.eyebrow}
      title={advantages.section.title}
      subtitle={advantages.section.subtitle}
    />

    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
      {advantages.cards.map((item) => (
        <FeatureCard title={`${item.value} — ${item.title}`} description={item.description} />
      ))}
    </div>
  </Container>
</section>
